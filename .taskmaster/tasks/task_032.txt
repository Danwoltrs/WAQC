# Task ID: 32
# Title: Fix Tin Sleeve Label Layout Issues and Missing Data
# Status: done
# Dependencies: 31
# Priority: high
# Description: Resolve four critical issues with tin sleeve label layout: move tracking number to left side with date below it, display Wolthers logo which is currently not showing, fix bags display showing N/A instead of actual count, and show complete quality description including client quality name and processing method.
# Details:
Fix tin sleeve label layout and data display issues in src/components/pdf/tin-sleeve-label.tsx and src/app/api/samples/[id]/print-tin-sleeve/route.tsx: 1) **Layout Fix**: Modify the header row structure to move tracking number to left side with date below it (currently tracking number is on right side). Update styles.headerRow to vertical layout and adjust positioning. 2) **Logo Display Fix**: Investigate why Wolthers logo (wolthers-logo-black.svg) is not showing on labels. The logo_url is being set correctly with base64 encoding, check if there are SVG rendering issues in @react-pdf/renderer. Consider converting SVG to PNG for better PDF compatibility. 3) **Bags Display Fix**: Fix the bags_display logic that's showing 'N/A' instead of actual bag counts. Update the bagsDisplay calculation in line 84-89 to properly handle cases where sample.bags or sample.bag_weight_kg exist but are not being displayed correctly. Ensure the query includes all necessary bag-related fields. 4) **Complete Quality Description**: Update the quality description display to show client quality name (custom_name from client_qualities) prominently followed by the full quality specification including processing method. Currently showing truncated descriptions like 'Alfenas Dulce - AD' instead of complete specs. Update the SQL query to include processing_method and other quality specification details from the quality_specifications table via client_qualities relationship.

# Test Strategy:
Test tin sleeve label generation with various sample scenarios: 1) Verify tracking number appears on left side with date below it in header section. 2) Confirm Wolthers logo displays correctly on all labels (test both SVG and PNG versions if needed). 3) Test bags display with different bag types: regular bags (should show 'X x Ykg'), bulk samples (should show 'Bulk (equiv. X bags)'), and edge cases with missing bag data. 4) Verify quality description shows client quality name prominently followed by complete quality specification including processing method. Test with samples that have client_qualities.custom_name set and samples without. 5) Generate test labels for samples with all data types (PSS, SS, Type Sample) and verify layout consistency across different content lengths.
